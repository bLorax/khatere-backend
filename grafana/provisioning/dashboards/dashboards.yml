apiVersion: 1

providers:
  - name: "khatere"
    orgId: 1
    folder: "Khatere"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
